"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Keeps');
const Fixture = require('../../fixtures/KeepsFixture');

describe('models:Keeps', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
