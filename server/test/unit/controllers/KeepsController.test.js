"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/KeepsController');

describe('controllers:KeepsController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
